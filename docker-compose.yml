services:
  mosquitto:
    image: eclipse-mosquitto:latest
    container_name: coolwatch-mqtt
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
    restart: unless-stopped

  app:
    build: ./app
    container_name: coolwatch-app
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
      - ./data:/data
    environment:
      - MQTT_HOST=mosquitto
      - MQTT_PORT=1883
      - DB_PATH=/data/coolwatch.db
      - RUN_SIMULATOR=${RUN_SIMULATOR:-true}   # set false when Pi is the publisher
      - CV_STREAM_URL=${CV_STREAM_URL:-http://192.168.179.15:8001/stream}
    depends_on:
      - mosquitto
    restart: unless-stopped

  cv:
    build: ./cv
    container_name: coolwatch-cv
    network_mode: host   # needs host network to reach Tailscale IPs (phone camera)
    volumes:
      - ./data:/data        # rois.json lives here
    environment:
      - MQTT_HOST=127.0.0.1  # mosquitto is exposed on host port 1883
      - MQTT_PORT=1883
      - CAMERA_URL=${CAMERA_URL:-http://192.168.179.18:8080/video}
      - ROOM=sendai_lab
      - ROIS_FILE=/data/rois.json
      - DIFF_THRESHOLD=0.05
      - INTERVAL=2.0
      - STREAM_PORT=8001
    restart: unless-stopped
